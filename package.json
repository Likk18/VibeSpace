{
    "name": "vibespace-v2",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "install:all": "npm install --prefix client && npm install --prefix server",
        "build:client": "npm run build --prefix client",
        "vercel-build": "npm run install:all && npm run build:client && node -e \"require('fs').cpSync('client/assets', 'client/dist/assets', {recursive: true})\"",
        "start": "node server/server.js"
    }
}